To make the api connect to your remote postgres database create a config.py file in the root folder witht the following structure:

host="your-db-host"
database="your-db-name"
user="your-user-name"
password="your-password"
port=your-port
